# Treat JAR and binary files as binary to prevent Git line ending corruption
*.jar binary
*.aar binary
gradle-wrapper.jar binary

# Ensure gradlew uses LF line endings
gradlew text eol=lf
gradlew.bat text eol=crlf
